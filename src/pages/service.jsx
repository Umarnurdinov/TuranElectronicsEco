import React from 'react';
import Header from '../components/header/header';

const service = () => {
    return (
        <div>
            <Header/>
            service
        </div>
    );
};

export default service;