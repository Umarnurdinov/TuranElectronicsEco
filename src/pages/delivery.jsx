import React from 'react';
import Header from '../components/header/header';

const delivery = () => {
    return (
        <div>
            <Header/>
            delivery
        </div>
    );
};

export default delivery;